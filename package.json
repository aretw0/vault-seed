{
  "name": "digital-gardening-kit",
  "version": "0.0.1",
  "description": "Kit de ferramentas para a jardinagem digital",
  "scripts": {
    "test": "jest",
    "format": "prettier --write \"**/*.md\"",
    "lint:main": "markdownlint \"*.md\" \"40 - Resources/**/*.md\" \"50 - Archives/**/*.md\" \"99 - Meta & Attachments/**/*.md\" --ignore \"00 - Inbox/**/*.md\" --config .markdownlint.json",
    "lint:docs": "cd docs && markdownlint \"**/*.md\"",
    "lint:templates": "cd \"90 - Templates\" && markdownlint \"**/*.md\"",
    "lint": "npm run lint:main && npm run lint:docs && npm run lint:templates",
    "release": "standard-version",
    "release:minor": "standard-version -- --release-as minor",
    "release:major": "standard-version -- --release-as major"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "markdownlint-cli": "^0.45.0",
    "prettier": "^3.6.2",
    "dotenv": "^16.4.5",
    "jest": "^29.7.0",
    "standard-version": "^9.5.0"
  }
}
